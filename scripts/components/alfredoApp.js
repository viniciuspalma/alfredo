/** @jsx React.DOM */

var React = require('React');

var AlfredoApp = React.createClass({
  render: function() {
    return (
      <div className="AlfredoApp">Hello</div>
    );
  }
});

module.exports = AlfredoApp;
